# Redis connection string used at runtime.
# On Flux, this is injected when a redis component is configured in flux.json.
# For local development, copy this file to .env and point it at your Redis instance.
REDIS_URL="redis://localhost:6379"
