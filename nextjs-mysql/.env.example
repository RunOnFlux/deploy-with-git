# MySQL connection string used by Prisma at runtime and during deploy migrations.
# On Flux, provide this as DATABASE_URL when configuring the managed database.
# For local development, copy this file to .env and point it at your MySQL instance.
DATABASE_URL="mysql://mysql:mysql@localhost:3306/nextjs_mysql"
