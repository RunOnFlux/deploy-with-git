{
  "name": "deploy-gatsby",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "gatsby build",
    "start": "gatsby serve --port ${PORT:-9000} --host 0.0.0.0",
    "dev": "gatsby develop"
  },
  "dependencies": {
    "gatsby": "^5.0.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
