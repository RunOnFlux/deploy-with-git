# PostgreSQL connection string used by Prisma at runtime and during deploy migrations.
# On Flux, this is injected automatically when a database component is configured in flux.json.
# For local development, copy this file to .env and point it at your Postgres instance.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/nextjs_demo?schema=public"
